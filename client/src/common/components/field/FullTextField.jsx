import { TextField } from '@mui/material';
import React from 'react';

const FullTextField = (props) => (
  <TextField fullWidth placeholder="Add Text" {...props} />
);

export default FullTextField;
